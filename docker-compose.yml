version: '3'

services:
  node:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:3000"
    networks:
      - nodeapp-network

  mongodb:
    image: mongo
    networks:
      - nodeapp-network

networks:
  nodeapp-network:
    driver: bridge
